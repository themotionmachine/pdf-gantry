"""Convert extracted PDF text to structured Markdown."""

from pathlib import Path


class MarkdownConverter:
    """Stub for converting extracted PDF text into Markdown.

    Will produce a structured .md file with headings, sections, and
    metadata derived from the PDF.
    """

    def convert(self, text: str, source: Path) -> str:
        """Convert plain extracted text to Markdown."""
        return ""
